import React from 'react';
import { render, screen } from '@testing-library/react';

import { Tabs } from './';

describe('Tabs', () => {
  test('renders', () => {
    expect(true).toEqual(false);
  });
});
